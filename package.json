{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@storybook/storybook-deployer": "^2.8.16",
    "turbo": "^1.6.3"
  }
}
